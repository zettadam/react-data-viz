const charcoal = '#36454f'
const lightgray = 'rgb(241,241,241)'

export const styles = {
  axis: {
    line: {
      stroke: charcoal,
      strokeWidth: 1
    },
    ticks: {
      stroke: charcoal
    }
  }
}
